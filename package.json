{
  "name": "catching-new-root",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --prefix CATCHING",
    "start": "npm run start --prefix CATCHING",
    "server": "npm run server --prefix CATCHING",
    "client": "npm run client --prefix CATCHING",
    "test": "npm run test --prefix CATCHING",
    "build": "npm run build --prefix CATCHING"
  }
}
